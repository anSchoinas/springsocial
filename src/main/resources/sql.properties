create.account=insert into Account (firstName, lastName, username, password) values (?, ?, ?, ?)
retrieve.account=select username, firstName, lastName from Account where username = ?
retrieve.facebook.connection.data=select userId, providerId, providerUserId, displayName, profileUrl, imageUrl, accessToken, secret, refreshToken, expireTime from " + tablePrefix + "UserConnection"